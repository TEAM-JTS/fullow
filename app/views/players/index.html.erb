<h1>Search</h1>
<br /><br /><br />


<h1>Player List</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @players.each do |player| %>
      <tr>
        <% @fullname = player.firstname.to_s + " " + player.lastname.to_s %>
        <td><%= link_to @fullname, player %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Player', new_player_path %>
